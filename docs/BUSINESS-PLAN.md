# SysMLEdge business plan (gated)

**As of:** 2026-09-11. **NARROW** lock: Elon / Horcrux stress-review. Normative product locks: [PRODUCT-LOCKS.md](PRODUCT-LOCKS.md). Foam proof (ALL four): [P1-acceptance.md](P1-acceptance.md). Living plan: [PRODUCT-PLAN.md](PRODUCT-PLAN.md).

This file is **not** a live price list and **not** a shipping claim. Runtime is not implemented in this seed.

---

## Verdict

Keep the Foam **contract** slice (bilingual bus). Freeze Pro / Team / SaaS editor / endgame AGI copy until Foam proof passes.

| Keep now | Not a product story until proof |
|---------|----------------------------------|
| `graph = model @ SHA`, MemNet one-way, STALE fail-closed, propose-only, no canvas | Pro beachhead, Team ACL, SaaS editor, AGI endgame as marketing |

**Load-bearing claim:** `graph = model @ SHA` + STALE fail-closed is valuable enough that solo SysML+Cursor desks switch and later pay ~US$19–29. **Asserted.** Foam proof + a second paid outsider are the gates.

**Cut:** Docs discipline ≠ product-market proof. Foam Phase-1 hours on P2 UI = kill signal.

---

## Competitor (pain lock)

**Kill:** tens-of-minutes **Cursor + git + grep/LSP → LLM** on the same Foam questions. That is the beachhead competitor — not Cameo, not a feature checklist.

**P1 win:** **wall-clock + less context**. Not feature count.

**Third axis:** structure fidelity. LLM-on-grep **always loses something** (silent drop). Score it: SysMLEdge GQL MUST preserve ownership / usage / impact edges and return `rev.sha` + `rev.stale=false`. Grep→LLM MAY omit; that is grep’s loss.

Proof scoring: [P1-acceptance.md](P1-acceptance.md) row 9. Locks: [PRODUCT-LOCKS.md](PRODUCT-LOCKS.md).

---

## Beachhead (asserted)

| Claim | Status |
|-------|--------|
| ~1k–10k textual SysML v2 + Cursor desks as TAM wedge | **Asserted, not evidenced** |
| Foam as first vertical | Pilot SoI named; proof not run |
| Solo desks switch, then pay Pro | Gated on proof **and** second paid outsider |

**Required after Foam:** a **second paid outsider**. Do not treat one Core-adjacent Foam demo as a market.

---

## Pricing (intent only; gated)

| Tier | Intent | Gate |
|------|--------|------|
| **Free** | 1 small project + MCP + soft caps | Sketch until Foam proof |
| **Pro** | ~US$19–29 / month | Foam proof **pass**, then **second paid outsider** before price experiments |
| **Team** | ~US$49–79 later, in-tenant ACL | P2; still frozen with P2 UI |

MUST NOT experiment on price, billing, or Pro packaging during the two-week Foam proof.

If proof **fails:** kill the Pro/beachhead story for now. Keep docs as a contract sketch or fold into MemNet tooling. MUST NOT ship “bilingual bus” as a product without the head-to-head.

If proof **passes:** KEEP narrowed — Foam vertical + MCP marketplace path. Price/experiment only after the second outsider. P2 UI still gated.

---

## Endgame (not this cut)

AGI / “agents own the model loop” is **endgame narrative only**. It is not a P1 deliverable, not a Pro feature list, and not a reason to spend Foam hours on a SaaS editor.

P2 SaaS editor and freemium UI remain a **gap** while P1 forbids that UI. Do not paper over it with AGI copy.

---

## Risks (document only)

1. Beachhead size is asserted. Without a second paid outsider after Foam, do not keep the paid-wedge story.
2. MemNet is **SPOF**. Thin or slow Foam projection kills GQL vs LSP; the head-to-head is the check.
3. “Beat grep” is soft until the timed head-to-head. Competitor = **tens-of-minutes grep**, not Cameo. MUST score **wall-clock + less context + structure fidelity** (silent drop on grep→LLM). Feature count is not a win. Freemium/Pro SaaS editor (P2) cannot close that gap in P1.

No extra mitigations beyond: run the four proof items; freeze the list in [PRODUCT-LOCKS.md](PRODUCT-LOCKS.md); fail closed on the product story if any item fails.

---

## Reject (business)

1. Selling Pro/Team or a SaaS editor before Foam proof ALL pass.
2. Treating Core or Foam-adjacent operators as the second paid outsider.
3. Cameo bake-offs as a two-week workstream (competitor is grep, not Cameo).
4. ClickUp / InvenTree as a SysMLEdge product to widen TAM.
5. AGI/endgame copy as a substitute for the head-to-head.
6. Shipping bilingual bus as a product if proof fails.
7. Parts-only SSOT, or treating parts/ports as a forever mapping cap.
8. Selling P1 as a feature-count win instead of wall-clock + less context + scored fidelity vs grep.
