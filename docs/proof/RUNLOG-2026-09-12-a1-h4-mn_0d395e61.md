# RUNLOG — 2026-09-12 — A1 H4 remeter (`mn_0d395e61`)

**Cite:** CEO Core **A1 green** on **`mn_0d395e61` only**. Claim pin = **depth=1 ego** (owner/contains) — **not** depth=2 sibling neighbourhood. Engine **memnet-llm 0.19.9**. Map SHA256 **`c2f16136e6f09f9a6c1ddf0026a15676f731575f1d2a1da6ccc2c464aa727bc3`** (SysMLEdge [#39](https://github.com/chouswei/SysMLEdge/pull/39)). `must_win_passed: true`. CueConflict **none**. `proof_pass_claimed: false`. `soft_pass: false`. Tip **untouched**. InvenTree **untouched**. **≠ Foam full green.**

**H1 root (this remeter):** Pi **restore** of the GitHub `fixtures/memnet-session.map`. Operator-narrow SCHEMA (`fields=id name kind role status recycle`) is a **soft-pass**, not bind. CI-pin [#39](https://github.com/chouswei/SysMLEdge/pull/39).

Pi artifact (Memnetor box; **not** copied into this repository). **MUST NOT invent** Arm A ms, Arm B ms, or Arm A hit counts.

**Do not mix:** **`mn_c7b75f2c`** is **not** this remeter. Stray **`mn_dbabac66`** **discarded**.

Compact status: [artifacts/RUNLOG-a1-h4-mn_0d395e61.json](artifacts/RUNLOG-a1-h4-mn_0d395e61.json).

## Gate

| Field | Value |
|-------|--------|
| Cut | A1 H4 remeter — BSI tips **Toolbar + Config** only |
| Lock | **A1 green** @ **`mn_0d395e61`**; claim pin **d=1 ego** (owner/contains) |
| Session | **`mn_0d395e61`** (not `mn_c7b75f2c`; not `mn_dbabac66`) |
| Engine | **memnet-llm 0.19.9** |
| Map | SHA256 **`c2f16136e6f09f9a6c1ddf0026a15676f731575f1d2a1da6ccc2c464aa727bc3`** ([#39](https://github.com/chouswei/SysMLEdge/pull/39)) |
| `must_win_passed` | **true** (context **P** · nodrop **P**; CueConflict **none**) |
| Wall | **F log-only** both BSI tips **warm+cold** (not A1 pass/fail) |
| `claim_text` | **BSI tips = depth=1 ego (owner/contains) — not depth=2 sibling neighbourhood** |
| `proof_pass_claimed` | **false** |
| `soft_pass` | **false** |
| Tip / InvenTree | **untouched** |
| Foam bind / row 9 / M5 | **not claimed** |
| Neo4j / dual-engine | **unused** |

## Exact Q table (Memnetor summary)

Arm A = grep `backgroundSetIndicator` (shared **A=4725** bytes). Arm B = depth=1 ego on each BSI tip. Warm and cold. **System skipped** (not in this remeter).

| seed | A bytes | B warm/cold bytes | B hits | ctx | nodrop | wall | CueConflict |
|------|--------:|-----------------:|-------:|:---:|:------:|:----:|:-----------:|
| `…Toolbar::backgroundSetIndicator` | **4725** | **~655B** warm/cold | **3** | **P** | **P** | **F** log-only | **none** |
| `…ConfigPanel::backgroundSetIndicator` | **4725** | **~649B** warm/cold | **3** | **P** | **P** | **F** log-only | **none** |

Gold qnames (existing Foam nested pair; **not** invented here):

- `FoamDetectionLiteVer2::CoreVideoMonitorToolbar::backgroundSetIndicator`
- `FoamDetectionLiteVer2::CoreMonitorConfigPanel::backgroundSetIndicator`

Arm A ms, Arm B ms, and Arm A hits were **not** in the fold dump. MUST NOT invent them. Bytes use the operator **~** where given.

## Per-Q P/F

| Q | context | nodrop | wall | CueConflict |
|---|---------|--------|------|-------------|
| Toolbar BSI | **P** | **P** | **F** log-only (warm+cold) | **none** |
| Config BSI | **P** | **P** | **F** log-only (warm+cold) | **none** |
| System | **skipped** | — | — | — |

## Axes (logged)

| Axis | Result | Note |
|------|--------|------|
| **context** | **P** ×2 | Toolbar **~655B** / Config **~649B** vs Arm A **4725B**. |
| **nodrop** | **P** ×2 | CueConflict **none**. B hits **3** ×2. |
| **must-win** | **true** | A1 pin = d=1 BSI ego (owner/contains). |
| **wall-clock** | **F log-only** | Both tips, warm+cold. **Not** an A1 pass/fail. Product bind H2H still logs wall as **P/F**. |
| **`claim_text`** | **pinned** | BSI tips = depth=1 ego (owner/contains) — not depth=2 sibling neighbourhood. |
| **`proof_pass`** | **false** | A1 green ≠ product `proof_pass`. MUST NOT set true. |
| **`soft_pass`** | **false** | H1 operator-narrow SCHEMA is the soft-pass; this remeter is **not** that. |
| **CueConflict** | **none** | Contrast: do **not** mix with `mn_c7b75f2c`. |
| **H1** | **root** | Pi restore of GitHub map; CI-pin [#39](https://github.com/chouswei/SysMLEdge/pull/39). |
| **cold** | **run** | Both BSI tips. Bytes **~655** / **~649** same as warm (operator ~). |
| **product / Foam full** | **not scored** | A1 BSI tips only. Tip Track A **untouched**. |

## Sessions (do not mix)

| Session | Role |
|--------|------|
| **`mn_0d395e61`** | **This remeter** — A1 green, d=1 ego |
| `mn_c7b75f2c` | **Not** this remeter |
| `mn_dbabac66` | **Discarded** |
| `mn_0d4f6178` | Foam Path-B / tip Track A — **untouched** |
| `mn_be03c1a9` | **(f)** clean / plumbing — **untouched** |
| `mn_27ce8714` | Dirty honesty archive — **untouched** |

## H1 (map)

LIVE SCHEMA MUST keep `qname` on PRT/POR/CON. Pi MUST restore GitHub `fixtures/memnet-session.map` @ SHA256 **`c2f16136e6f09f9a6c1ddf0026a15676f731575f1d2a1da6ccc2c464aa727bc3`**. Operator-narrow SCHEMA is a **soft-pass**, not bind. This H4 remeter is **after** that restore.

## Next

- **No** `proof_pass` / Foam full green from A1 BSI tips
- **No** mix `mn_c7b75f2c` / `mn_dbabac66` into this lock
- **No** treat wall **F** as an A1 fail (log-only on this pin)
- Product bind H2H + cold still required for `proof_pass_claimed`
- Tip sessions **untouched**
- InvenTree **untouched**

## Kill theater (do not sell)

- A1 green as **P1 / M5 / row 9** / `proof_pass_claimed`
- Foam **full** green (System skipped; product bind H2H still open)
- Wall **F** as A1 fail, as product pass, or as a Neo4j reopen
- `mn_c7b75f2c` or discarded `mn_dbabac66` as this green sid
- H1 operator-narrow SCHEMA as bind / as this remeter
- Tip `mn_0d4f6178` Track A as this table
- p1-tiny / `mn_be03c1a9` plumbing as the Foam wedge
- InvenTree as touched
- Depth=2 sibling neighbourhood as the A1 claim
- Neo4j / dual-engine reopen

## Explicit non-claims

- Not `proof_pass` / `proof_pass_claimed`
- Not `soft_pass`
- Not Foam full green / not product bind H2H pass
- Not tip H2H (`mn_0d4f6178`)
- Not plumbing H2H (`mn_be03c1a9`)
- Not InvenTree / ClickUp product work
- Not Neo4j / dual-engine / C rewrite
- Not continuous same-session **(g)** (**(r)** deferred; **(g)** provisional)
